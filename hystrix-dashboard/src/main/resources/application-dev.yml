spring:
  application:
    name: hystrix-dashboard
server:
  port: 18004
eureka:
  client:
    service-url:
      defaultZone: http://localhost:18000/eureka
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always